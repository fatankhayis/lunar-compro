{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "build:spa": "node -e \"const {spawnSync}=require('child_process');const path=require('path');process.env.BUILD_TO_BACKEND='true';const fe=path.join('..','Front-End');const cmd='npm --prefix \\\"'+fe+'\\\" run build';const r=spawnSync(cmd,{stdio:'inherit',shell:true,env:process.env});process.exit(r.status ?? 1);\""
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
